#!/bin/zsh
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
APP_ROOT="$(cd "$SCRIPT_DIR/../../../.." && pwd)"

cd "$APP_ROOT"
export SYLLABUS_BUNDLE_LAUNCH=1
exec /usr/bin/env python3 "$APP_ROOT/main.py"
